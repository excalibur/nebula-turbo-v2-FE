// 进行登录控制
define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var login = function(actions){
            console.log("=======login=======");
            console.log(actions);
        }

    return login;
});
