/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var WorkspaceRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return WorkspaceRouter;
});
