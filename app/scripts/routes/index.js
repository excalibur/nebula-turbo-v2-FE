define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var index = function(actions){
            console.log("==============");
            console.log(actions);
        }

    return index;
});
